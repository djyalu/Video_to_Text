{
  "en": "of any type.Because it's using a.while loop an line 12 E this code would actually loop through all of the user's\n\nifluserSysID[ throw new Error Invalid user sys_id.passed in\n\ngrTask.addActiveQuery; grTask,addQuery(opened_by,userSysID) grTask.query; whilegTask.nex)\n\nreturnticketNumber; Fig.2.03 This.code has a few problems.First,it assumes that a given.user.would.only have created a single open task open tickets and return the tieket nunber of the last ticket the query finds Even if we switch this while to an if biock though,we would still only return the first such ticket in the database. In addition to being extremely inefficientn,this code will almost certainly result in unexpected and unpredictable results. This specific use-case is probably something that needs to be re-thought in general, but we can make some drastic improvements with just a few changes: First,significantly refining the query to ensure that ifwe only expect one record,we actually do onlyget one record. Adding setLimit1 and changing the while to an if block in also a good idea, to ensure that we do indeed only gt one record even if more records matching our query exist in the database.This would also drastically improve our performance since the query runner would stop seeking through the t es t  soo me wf ces",
  "ko": "어떤 유형이든 상관없습니다. 12E 라인에서 a.while 루프를 사용하기 때문에 이 코드는 실제로 사용자의 모든 항목을 반복합니다.\n\nifluserSysID[ 새로운 오류 발생 잘못된 사용자 sys_id.passed in\n\ngrTask.addActiveQuery; grTask,addQuery(opened_by,userSysID) grTask.query; whilegTask.nex)\n\n왕복티켓번호; 그림 2.03 이.코드에는 몇 가지 문제가 있습니다. 첫째, 주어진.user.가 단일 열린 작업 열린 티켓만 생성하고 쿼리가 찾은 마지막 티켓의 타이켓 번호를 반환한다고 가정합니다. 비록 이것을 if biock으로 전환하더라도 여전히 데이터베이스에서 첫 번째 티켓만 반환합니다. 극도로 비효율적일 뿐만 아니라, 이 코드는 예상치 못한 예측할 수 없는 결과를 가져올 것이 거의 확실합니다. 이 특정 사용 사례는 아마도 일반적으로 다시 생각해야 할 것이지만 몇 가지 변경만으로 대폭적인 개선을 이룰 수 있습니다. 첫째, 하나의 레코드만 예상하면 실제로는 하나의 레코드만 가져오도록 쿼리를 대폭 개선합니다. setLimit1을 추가하고 if 블록에 while을 변경하는 것도 좋은 생각입니다. 쿼리와 일치하는 더 많은 레코드가 데이터베이스에 존재하더라도 실제로 하나의 레코드만 GT로 수행하도록 보장하는 것입니다. 쿼리 실행기가 테스트를 통해 탐색을 중단하므로 성능도 크게 향상됩니다.",
  "codes": [
    " function getOpenTicketNumberByUser(userSysID)",
    "  var ticketNumber\n  var grTask=new GlideRecord(task",
    "   );",
    "    ticketNumber=grTask.getValue(number);"
  ]
}